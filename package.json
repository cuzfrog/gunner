{
  "name": "gunner",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "trace": "bun run scripts/sim-trace.ts",
    "preview": "bun run build.ts && bun run preview.ts",
    "sync-html-version": "bun run scripts/sync-html-version.ts"
  },
  "dependencies": {
    "awilix": "13.0.5"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
