{
  "name": "gunner",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "preview": "bun run build.ts && bun run preview.ts"
  },
  "dependencies": {
    "awilix": "13.0.5"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
