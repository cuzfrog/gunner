{
  "name": "gunner",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "preview": "python3 -m http.server -d dist 8080"
  },
  "dependencies": {
    "awilix": "13.0.5"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
